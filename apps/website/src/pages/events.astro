---
import Layout from '../layouts/Layout.astro'
import { EventsPage } from '../components/EventsPage'
import { getEvents } from '../utils/getEvents'

const events = await getEvents()
---

<Layout title="Looney - Events">
  <EventsPage events={events} />
</Layout>
