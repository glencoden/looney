{
    "name": "looney-monorepo",
    "version": "1.0.0",
    "scripts": {
        "build": "turbo build",
        "dev": "dotenv -- turbo dev",
        "lint": "prettier --write . && turbo lint",
        "typecheck": "turbo typecheck",
        "db:start": "supabase start -x gotrue,realtime,storage-api,imgproxy,edge-runtime,mailpit,logflare,vector,supavisor",
        "db:stop": "supabase stop",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "glencoden",
    "license": "ISC",
    "packageManager": "pnpm@10.33.0",
    "engines": {
        "node": ">=24.0.0"
    },
    "devDependencies": {
        "dotenv": "catalog:",
        "dotenv-cli": "catalog:",
        "prettier": "^3.8.1",
        "prettier-plugin-tailwindcss": "^0.6.14",
        "turbo": "^2.9.5"
    }
}
