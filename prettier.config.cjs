/** @type {import('prettier').Config} */
const config = {
    tabWidth: 4,
    semi: false,
    singleQuote: true,
    jsxSingleQuote: true,
    plugins: ['prettier-plugin-tailwindcss'],
}

module.exports = config
